---
python_major: 3
python_minor: 7
python_version: "{{ python_major }}.{{ python_minor }}"
python_version_short: "{{ python_major }}{{ python_minor }}"
python_prefix: "py{{ python_version_short }}"
matrix_packages:
  - name: "{{ python_prefix }}-matrix-synapse"
  - name: "{{ python_prefix }}-matrix-synapse-ldap3"
